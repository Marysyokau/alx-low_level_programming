#include<stdio.h>

/**
 * main - Entry point
 * puts(programming is like putting a puzzle)
 * Return: Always 0(Success)
 */
int main(void)
{
puts(\"Programming is like building a multilingual puzzle");
return (0);		
}
