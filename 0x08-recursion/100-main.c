#include "main.h"
#include <stdio.h>

/**
 * is_palindrome - determines an empty string
 * @s: string input
 * Return: returns 1 if a string is a palindrome and 0 if not
 */

int is_palindrome(char *s)
{
	int len;
	len = strlen(s);

	if (len == 0)
	{

	}
}
